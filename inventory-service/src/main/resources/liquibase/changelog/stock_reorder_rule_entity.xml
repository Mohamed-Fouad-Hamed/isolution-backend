<?xml version="1.0" encoding="utf-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.4.xsd">

    <property name="now" value="now()" dbms="mysql,h2"/>
    <property name="now" value="current_timestamp" dbms="postgresql"/>
    <property name="now" value="sysdate" dbms="oracle"/>

    <property name="autoIncrement" value="true" dbms="mysql,h2,postgresql,oracle"/>

    <property name="floatType" value="float4" dbms="postgresql, h2"/>
    <property name="floatType" value="float" dbms="mysql, oracle"/>


    <changeSet id="0017" author="MohamedFouad" context="dev, prod">

        <sql>

            CREATE TABLE stock_reorder_rule (

            id BIGSERIAL PRIMARY KEY,

            product_id BIGINT NOT NULL,

            location_id BIGINT NOT NULL,

            min_qty NUMERIC(18,4),
            max_qty NUMERIC(18,4),

            order_qty NUMERIC(18,4),

            CONSTRAINT fk_rule_product
            FOREIGN KEY (product_id)
            REFERENCES product(id),

            CONSTRAINT fk_rule_location
            FOREIGN KEY (location_id)
            REFERENCES stock_location(id)
            );


        </sql>

    </changeSet>
</databaseChangeLog>