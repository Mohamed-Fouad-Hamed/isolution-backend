<?xml version="1.0" encoding="utf-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.4.xsd">


    <!-- Create entity user role -->
    <changeSet id="00009" author="MohamedFouad" context="dev, prod">

        <sql>
            CREATE TABLE IF NOT EXISTS user_role
            (
                user_id BIGINT NOT NULL,
                role_id integer NOT NULL,
                CONSTRAINT user_role_p_key PRIMARY KEY (user_id, role_id),
                CONSTRAINT fk_role_user FOREIGN KEY (role_id)
                REFERENCES role (id) MATCH SIMPLE
                ON UPDATE NO ACTION
                ON DELETE NO ACTION,
                CONSTRAINT fk_user_role FOREIGN KEY (user_id)
                REFERENCES app_users (id) MATCH SIMPLE
                ON UPDATE NO ACTION
                ON DELETE NO ACTION
            );

            INSERT INTO user_role(
            user_id, role_id)
            VALUES (1, 1);

        </sql>
    </changeSet>
</databaseChangeLog>